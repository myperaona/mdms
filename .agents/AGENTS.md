# Project Agent Rules

## Language & Plan Directives
- **Implementation Plan Language**: Always write implementation plans (`implementation_plan.md`) in Korean (한글).

## Git & Commit Directives
- **Explicit Request Only**: Do NOT run `git commit` or `git push` unless the user explicitly requests it in the current turn.
